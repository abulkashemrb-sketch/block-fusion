/// The set of colors a placed block cell can have.
///
/// Kept separate from any rendering concerns — the game layer maps each
/// value to an actual [Color] for painting.
enum BlockColor { red, orange, yellow, green, teal, blue, purple }
